import React from 'react';
import PropTypes from 'prop-types';

function FriendDogForm(props) {
  const {dog} = props;
  
  function handleEditDogFormSubmission(event){
    
    
  }
}

export default FriendDogForm